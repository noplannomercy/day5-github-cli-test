// 유틸리티 함수
function add(a, b) {
  return a + b;
}

function subtract(a, b) {
  return a - b;
}

function multiply(a, b) {
  return a * b;
}

// 버그 있음: 나눗셈 시 0 체크 없음
function divide(a, b) {
  return a / b;
}
